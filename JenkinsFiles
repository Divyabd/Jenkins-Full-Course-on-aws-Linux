pipeline{
    agent any
        stages{
            stage("sonartest"){
                steps{
                    withSonarQubeEnv('sonarQube') {
       sh 'mvn -f Maven/pom.xml clean install package sonar:sonar'
}
                }
            }
        }
    
}